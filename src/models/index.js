const { Candle } = require('./candle');
const { SwingLevel } = require('./swingLevel');
const { Signal } = require('./signal');
const { Trade } = require('./trade');

module.exports = {
	Candle,
	SwingLevel,
	Signal,
	Trade
};

